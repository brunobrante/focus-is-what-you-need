// The accent for the "linked instance" state, shared across components, System
// Design tokens, and References so the linkable model reads the same everywhere
// (a component instance, a linked token, and a linked reference all show this
// purple). See the linkable model in Product.md / Architecture.md.
export const LINKED_INSTANCE_COLOR = "#8638E5";
export const LINKED_INSTANCE_HOVER = "rgba(134, 56, 229, 0.55)";
