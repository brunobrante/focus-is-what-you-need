# Inspector — Guides (grid / layout guides)

Status: **placeholder** — intentionally empty. To be defined later.

Reserved for the Guides panel (grid, columns, layout guides). The product already has a
related concept in `Product.md` → "Parent grid guidance" (a grid/guides derived from an
ancestor when editing a component in isolation); this panel is where that — and any
explicit grid/guide controls — will be specified.

Nothing decided yet. Fill this in when the approach is chosen.
